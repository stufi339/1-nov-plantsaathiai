# Field Data Caching - Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     User Opens Field Page                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Load Field Data from localStorage                   │
│                  (field_${fieldId}_data)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│           Check Cache Status (fieldDataCacheService)             │
│              (field_cache_${fieldId})                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │  Cache Valid?    │      │  No Cache?       │
    │  (< 24 hours)    │      │  (First visit)   │
    └────────┬─────────┘      └────────┬─────────┘
             │                         │
             ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │ Load Cached Data │      │ Show Fetch       │
    │ Show Green       │      │ Button (Blue)    │
    │ Indicator        │      │                  │
    └────────┬─────────┘      └────────┬─────────┘
             │                         │
             │                         ▼
             │                ┌──────────────────┐
             │                │ User Clicks      │
             │                │ Fetch Button     │
             │                └────────┬─────────┘
             │                         │
             │                         ▼
             │                ┌──────────────────┐
             │                │ Fetch Satellite  │
             │                │ Data from APIs   │
             │                └────────┬─────────┘
             │                         │
             │                         ▼
             │                ┌──────────────────┐
             │                │ Save to Cache    │
             │                │ (24h expiry)     │
             │                └────────┬─────────┘
             │                         │
             └─────────────────────────┘
                             │
                             ▼
                ┌────────────────────────┐
                │  Display Field Data    │
                │  with Status Indicator │
                └────────────────────────┘
```

## Cache Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                        Cache Lifecycle                           │
└─────────────────────────────────────────────────────────────────┘

Time: 0h (Day 1, 10:00 AM)
├─ Event: User fetches satellite data
├─ Action: Data saved to cache
├─ Expiry: Set to 24 hours from now
└─ Status: ✅ Fresh (24h remaining)

Time: 12h (Day 1, 10:00 PM)
├─ Event: User opens field
├─ Action: Load from cache
├─ Expiry: 12 hours remaining
└─ Status: ✅ Fresh (12h remaining)

Time: 23h (Day 2, 9:00 AM)
├─ Event: User opens field
├─ Action: Load from cache
├─ Expiry: 1 hour remaining
└─ Status: ✅ Fresh (1h remaining)

Time: 24h+ (Day 2, 10:01 AM)
├─ Event: User opens field
├─ Action: Load from cache (for continuity)
├─ Expiry: Expired
└─ Status: 🔄 Can refresh

Time: 24h+ (Day 2, 10:05 AM)
├─ Event: User clicks "Update Satellite Data"
├─ Action: Fetch fresh data, update cache
├─ Expiry: Set to 24 hours from now
└─ Status: ✅ Fresh (24h remaining)
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         Data Sources                             │
└─────────────────────────────────────────────────────────────────┘

Satellite APIs
├─ SoilAnalysisService
├─ satelliteDataService
└─ geeService
        │
        ▼
┌─────────────────┐
│ Fetch Data      │
│ - NDVI          │
│ - Vegetation    │
│ - Soil Props    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Process Data    │
│ - Calculate     │
│ - Analyze       │
│ - Format        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Cache Service   │
│ - Validate      │
│ - Store         │
│ - Expire        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ localStorage    │
│ field_cache_*   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ UI Display      │
│ - Indicators    │
│ - Data Cards    │
│ - Actions       │
└─────────────────┘
```

## User Decision Tree

```
                    User Opens Field
                           │
                           ▼
                    ┌──────────────┐
                    │ Is there     │
                    │ cached data? │
                    └──────┬───────┘
                           │
                ┌──────────┴──────────┐
                │                     │
               YES                   NO
                │                     │
                ▼                     ▼
        ┌──────────────┐      ┌──────────────┐
        │ Is cache     │      │ Show "Fetch  │
        │ valid?       │      │ Data" button │
        │ (< 24h)      │      └──────┬───────┘
        └──────┬───────┘             │
               │                     │
        ┌──────┴──────┐              │
        │             │              │
       YES           NO              │
        │             │              │
        ▼             ▼              │
┌──────────────┐ ┌──────────────┐   │
│ Show green   │ │ Show orange  │   │
│ "Up-to-date" │ │ "Can refresh"│   │
│ indicator    │ │ button       │   │
└──────────────┘ └──────┬───────┘   │
                        │           │
                        └───────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ User clicks      │
                    │ fetch/refresh    │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Fetch satellite  │
                    │ data from APIs   │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Save to cache    │
                    │ (24h expiry)     │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Show green       │
                    │ "Up-to-date"     │
                    │ indicator        │
                    └──────────────────┘
```

## Cache Storage Structure

```
localStorage
│
├─ field_123_data (Field metadata)
│  ├─ id: "123"
│  ├─ name: "North Field"
│  ├─ coordinates: [...]
│  ├─ cropType: "Rice"
│  ├─ health: { ndvi: 0.72, ... }
│  └─ quadrants: [...]
│
├─ field_cache_123 (Cached satellite data)
│  ├─ fieldId: "123"
│  ├─ lastUpdated: "2024-10-31T10:00:00Z"
│  ├─ expiresAt: "2024-11-01T10:00:00Z"
│  └─ data:
│     ├─ health: { ndvi: 0.72, status: "Excellent", ... }
│     ├─ quadrants: [...]
│     └─ comprehensiveAnalysis: { ... }
│
├─ field_456_data
│  └─ ...
│
└─ field_cache_456
   └─ ...
```

## Status Indicator Logic

```
┌─────────────────────────────────────────────────────────────────┐
│                    Status Indicator Logic                        │
└─────────────────────────────────────────────────────────────────┘

Condition: field.health.ndvi === 0
├─ Display: 🔵 Blue "Fetch Data" button
└─ Action: First-time data fetch

Condition: cache.valid === true && timeRemaining > 0
├─ Display: 🟢 Green "Data is up-to-date"
├─ Info: "Next update in {timeRemaining}"
└─ Action: None (data is fresh)

Condition: cache.valid === false && field.health.ndvi > 0
├─ Display: 🟠 Orange "Can refresh" button
├─ Info: "Satellite data can be refreshed"
└─ Action: Manual refresh available

Condition: isLoadingIndices === true
├─ Display: ⏳ Loading spinner
├─ Info: "Fetching Satellite Data..."
└─ Action: Wait for completion
```

## API Call Optimization

```
┌─────────────────────────────────────────────────────────────────┐
│                    API Call Comparison                           │
└─────────────────────────────────────────────────────────────────┘

WITHOUT CACHING:
Day 1: User visits field 5 times
├─ Visit 1: API call ✓
├─ Visit 2: API call ✓
├─ Visit 3: API call ✓
├─ Visit 4: API call ✓
└─ Visit 5: API call ✓
Total API Calls: 5

WITH CACHING:
Day 1: User visits field 5 times
├─ Visit 1: API call ✓ (cache created)
├─ Visit 2: Cache hit ✓
├─ Visit 3: Cache hit ✓
├─ Visit 4: Cache hit ✓
└─ Visit 5: Cache hit ✓
Total API Calls: 1

Reduction: 80% (4 out of 5 calls saved)

OVER 30 DAYS:
Without Caching: 150 API calls (5 visits/day × 30 days)
With Caching: 30 API calls (1 call/day × 30 days)
Reduction: 80% (120 calls saved)
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      Error Handling                              │
└─────────────────────────────────────────────────────────────────┘

Fetch Satellite Data
        │
        ▼
┌──────────────────┐
│ Try API Call     │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
 Success    Error
    │         │
    ▼         ▼
┌────────┐ ┌────────────────┐
│ Cache  │ │ Show Error     │
│ Data   │ │ Toast          │
└────┬───┘ └────────┬───────┘
     │              │
     ▼              ▼
┌────────┐ ┌────────────────┐
│ Show   │ │ Keep Old Cache │
│ Success│ │ (if exists)    │
│ Toast  │ └────────────────┘
└────────┘

Cache Retrieval
        │
        ▼
┌──────────────────┐
│ Try Load Cache   │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
 Success    Error
    │         │
    ▼         ▼
┌────────┐ ┌────────────────┐
│ Return │ │ Return null    │
│ Data   │ │ Log error      │
└────────┘ └────────────────┘
```

## Summary

The caching system provides:
- ✅ Efficient data management
- ✅ Clear user feedback
- ✅ Reduced API calls
- ✅ Better performance
- ✅ Graceful error handling

All flows are designed to provide the best user experience while optimizing system resources.
